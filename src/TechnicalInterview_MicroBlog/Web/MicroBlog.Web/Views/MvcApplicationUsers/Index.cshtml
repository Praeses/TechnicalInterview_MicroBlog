@model IEnumerable<MicroBlog.Web.Models.ViewModel.ApplicationUserViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { applicationUserId = item.ApplicationUserId }) |
            @Html.ActionLink("Details", "Details", new { id = item.ApplicationUserId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ApplicationUserId })
        </td>
    </tr>
}

</table>
